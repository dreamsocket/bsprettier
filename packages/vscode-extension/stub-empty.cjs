// Stub for deps pulled transitively by brighterscript but never used on the
// format path (deployment + TS compiler). Verified byte-identical formatting
// across the full corpus.
module.exports = {};
